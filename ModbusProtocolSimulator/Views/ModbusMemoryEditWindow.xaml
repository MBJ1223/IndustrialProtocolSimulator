<Window x:Class="ModbusProtocolSimulator.Views.ModbusMemoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modbus 메모리 편집" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="선택된 메모리 영역에 값을 쓰기합니다."
                   Margin="0,0,0,15" FontWeight="Bold"/>

        <Label Grid.Row="1" Grid.Column="0" Content="현재 영역:" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="AreaTypeText" VerticalAlignment="Center" FontWeight="Bold"/>

        <Label Grid.Row="2" Grid.Column="0" Content="주소:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="AddressTextBox" Margin="0,5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="값:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ValueTextBox" Margin="0,5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" x:Name="HintText"
                   Foreground="Gray" FontSize="11" TextWrapping="Wrap" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="쓰기" Width="80" Click="WriteButton_Click" Background="#FF4CAF50" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="닫기" Width="80" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
