<Window x:Class="S7ProtocolSimulator.Views.S7MemoryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="메모리 값 편집" Height="220" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Content="영역:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="AreaCombo" Margin="0,0,0,5">
            <ComboBoxItem Content="I (입력)"/>
            <ComboBoxItem Content="Q (출력)"/>
            <ComboBoxItem Content="M (머커)" IsSelected="True"/>
            <ComboBoxItem Content="DB"/>
        </ComboBox>

        <Label Grid.Row="1" Content="DB번호:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DbNumberTextBox" Margin="0,0,0,5" Text="1"/>

        <Label Grid.Row="2" Content="주소:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="AddressTextBox" Margin="0,0,0,5" Text="0"/>

        <Label Grid.Row="3" Content="값:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ValueTextBox" Margin="0,0,0,5" Text="0"/>

        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Foreground="Gray" FontSize="11"
                   Text="바이트 단위 값 (0~255)" Margin="0,5"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="쓰기" Width="70" Click="WriteButton_Click" Margin="0,0,5,0"/>
            <Button Content="닫기" Width="70" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
